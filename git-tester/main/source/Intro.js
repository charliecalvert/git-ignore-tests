import React from 'react';

function Intro() {
    return (
        <React.Fragment>
            <h1>React Hooks Git Ignore Tests</h1>

            <p>Midterm prototype code with function components and hooks with Docker hot reloading.</p>

            <h2>Menu</h2>
        </React.Fragment>
    );
}

export default Intro;
