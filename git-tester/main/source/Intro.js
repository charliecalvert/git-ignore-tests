import React from 'react';

export default class Intro extends React.Component {
    render() {
        return (
            <React.Fragment>
                <h1>Git Ignore Tests Router Dom</h1>

                <p>Midterm prototype code with React Router DOM and Docker Hot Reloading.</p>

                <h2>Menu</h2>
            </React.Fragment>
        );
    }
}
